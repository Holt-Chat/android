# Copy this file to app/keystore.properties (which is gitignored) and fill in your
# signing key to produce signed release builds. Without it, release builds fall back
# to the debug signing key. storeFile is resolved relative to the app/ module directory.
storeFile=sideload.keystore
storePassword=CHANGEME
keyAlias=holt
keyPassword=CHANGEME
